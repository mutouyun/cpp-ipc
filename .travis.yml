language: cpp
dist: xenial

matrix:
  include:
    - os: linux
      compiler: gcc

install:
  - sudo apt-get update
  - sudo apt-get install libqt5test5 qt5-qmake

script:
  - gcc -v
  - qmake -v

notifications:
  slack:
    on_success: never
    on_failure: never
